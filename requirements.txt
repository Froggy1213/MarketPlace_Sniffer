# --- Core & Web Framework ---
fastapi>=0.100.0
uvicorn[standard]>=0.23.0  # Сервер для запуска FastAPI
pydantic-settings>=2.0.0   # Удобное управление конфигами (.env)

# --- Parser Engine ---
playwright>=1.40.0         # Наш браузерный движок

# --- Database (Async) ---
sqlalchemy>=2.0.0          # ORM для работы с базой
asyncpg>=0.28.0            # Асинхронный драйвер для PostgreSQL
greenlet>=3.0.0            # Нужно для работы SQLAlchemy в асинхронном режиме

# --- Telegram Bot ---
aiogram>=3.0.0             # Асинхронная библиотека для ТГ

# --- Task Scheduling ---
apscheduler>=3.10.0        # Планировщик задач (запуск парсера раз в N минут)

# --- Utilities ---
python-dotenv>=1.0.0       # Загрузка переменных окружения
beautifulsoup4             # Иногда полезно для допарсинга HTML (легче, чем Playwright для мелких задач)
lxml                       # Быстрый парсер XML/HTML для bs4